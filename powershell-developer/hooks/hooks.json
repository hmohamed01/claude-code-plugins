{
  "description": "PowerShell unsafe coding pattern detection hooks. Warns on writes containing aliases, missing CmdletBinding, hardcoded credentials, and other anti-patterns.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-powershell-patterns.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
