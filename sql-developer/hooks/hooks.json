{
  "description": "SQL Server unsafe coding pattern detection hooks. Warns on writes containing SQL injection risks, NOLOCK without comments, missing error handling, and other anti-patterns.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-sql-patterns.sh \"$FILE_PATH\" \"$TOOL_INPUT\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
