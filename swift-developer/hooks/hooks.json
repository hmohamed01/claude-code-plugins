{
  "description": "Swift unsafe coding pattern detection hooks. Blocks writes containing force unwraps, hardcoded secrets, main thread blocking, and unsafe Sendable patterns.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-swift-patterns.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
